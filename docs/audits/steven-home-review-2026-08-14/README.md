# Steven Home Directory Review — 2026-08-14

This is a read-only structural review of `/Users/steven`, performed with the
local `ecosystem-intelligence` and `eza-nav` guidance in
`~/.agent-skills`.

## Executive summary

The home directory is an active personal, creative, software, and AI-agent
ecosystem rather than a simple project folder. It contains a large number of
top-level projects, parallel AI-tool runtimes, archives/quarantines, and Git
repositories.

The most important current signals are:

- `Downloads` is approximately 24 GB.
- `Documents` is approximately 3.2 GB.
- `CoX-mod-Adventure` is approximately 4.3 GB.
- `.codex` is approximately 668 MB and `.claude` approximately 568 MB.
- `.agent-skills` is approximately 123 MB.
- Git repositories are distributed across `github/`, `CoX-mod-Adventure/`,
  `ESO/`, `PYTHON_MARKETPLACE_MASTER/`, `diGiTaLdiVe/`, and many other roots.

## Scope and limits

Reviewed directory names, metadata, selected directory sizes, runtime paths,
and Git repository locations. File contents, credentials, messages, and
personal documents were not inspected. Sizes are filesystem snapshots and can
change while work continues.

## Relationship to prior audit

The earlier durable audit is
`docs/audits/steven-eza-home-audit-2026-08-08/`. It remains useful as a
historical Eza snapshot. This review updates the interpretation and selected
measurements but does not overwrite that prior evidence.

See [findings.md](findings.md) for detailed observations and follow-ups.
